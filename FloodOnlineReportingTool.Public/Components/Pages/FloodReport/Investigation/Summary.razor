@page "/floodreport/investigation/summary"
@using Microsoft.AspNetCore.Components.Authorization

<PageTitle>@InvestigationPages.Summary.Title</PageTitle>

<GdsHeading Level="1">@InvestigationPages.Summary.Title</GdsHeading>

<AuthorizeView>
    <NotAuthorized>
        <AccessDenied />
    </NotAuthorized>
    <Authorized>
        <GdsSpinner Show="_isLoading" ShowMessage="true" />

        @if (!_isLoading)
        {
            if (_summaryErrors.Count > 0)
            {
                <div class="govuk-error-summary" data-module="govuk-error-summary">
                    <div role="alert">
                        <h2 class="govuk-error-summary__title">There is a problem</h2>
                        <div class="govuk-error-summary__body">
                            <ul class="govuk-list govuk-error-summary__list">
                                @foreach (var error in _summaryErrors)
                                {
                                    <li>@error</li>
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            }

            <InvestigationDtoSummary InvestigationDto="@_investigationDto" IsInternal="@_isInternal" ValidationStatusChanged="OnValidationStatusChanged" />

            @if (_summaryErrors.Count == 0)
            {
                <GdsHeading Level="2">Now send the investigation</GdsHeading>
                <p>You are confirming that, to the best of your knowledge, the details you are providing are correct.</p>
                <p>You also agree that the information provided may be used by flood authorities to help with work to better understand flooding.</p>
                <p><GdsButton IsSubmit ="false" PreventDoubleClick  OnClick="OnAcceptAndSend" Text="Accept and send" /></p>
            }
        }
    </Authorized>
</AuthorizeView>